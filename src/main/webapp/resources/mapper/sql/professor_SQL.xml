<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="professor">
	
	<select id="selectProfessorPassword" resultType="String" parameterType="Integer">
		<![CDATA[
			SELECT
			    password
			FROM
			    professor
			WHERE
				professor_id = #{professor_id}
		]]>
	</select>
	<select id="selectProfessorList" resultType="hashmap" parameterType="Integer">
		<![CDATA[
			SELECT
			    *
			FROM
			    professor
			WHERE
				professor_id = #{professor_id}
		]]>
	</select>
	<select id="selectMenteeList" resultType="hashmap" parameterType="Integer">
		<![CDATA[
			SELECT
			    *
			FROM
			    student
			WHERE
				professor_id = #{professor_id}
		]]>
	</select>
	<select id="selectObjectiveQuestionList" resultType="java.util.LinkedHashMap" parameterType="String">
		<![CDATA[
			SELECT
			    *
			FROM
			    ObjectiveQuestion
			WHERE
				course_id = #{course_id}
		]]>
	</select>
	<select id="selectSubjectiveQuestionList" resultType="java.util.LinkedHashMap" parameterType="String">
		<![CDATA[
			SELECT
			    *
			FROM
			    SubjectiveQuestion
			WHERE
				course_id = #{course_id}
		]]>
	</select>
</mapper>